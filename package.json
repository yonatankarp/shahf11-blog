{
  "name": "shahf11-blog",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "copy:images": "node scripts/copy-images.mjs",
    "dev": "npm run copy:images && astro dev",
    "build": "npm run copy:images && astro build",
    "preview": "astro preview",
    "typecheck": "astro check",
    "check:blog-assets": "node scripts/check-blog-assets.mjs",
    "check:site-metadata": "node scripts/check-site-metadata.mjs",
    "visual:capture": "node scripts/capture-screenshots.mjs",
    "check": "npm run check:blog-assets && npm run build && npm run check:site-metadata"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.0",
    "@fontsource-variable/assistant": "^5.0.0",
    "@fontsource-variable/frank-ruhl-libre": "^5.0.0",
    "astro": "^7.0.5",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
